```mermaid
graph TD;
  MASTER-->dev;
  dev-->user_master;
  user_master-->devise;
  dev-->product_master;
  dev-->mailer_master;
  dev-->admin_master;
  dev-->kit_ui;
  dev-->stripe;
  dev-->update_db;
  dev-->imafe_hosting;
  dev-->ajaxify;
  dev-->break_stuff;
```
